# DESIGN.md — 挪車的代價

## 世界：夜間停車場（The Garage, After Hours）

不是「科技 demo 頁」，是**深夜的機械式停車場現場**。頁面的一切材料都取自停車場自己的世界：
柏油、混凝土、地坪漆、標線、票根。3D 場景是主角，UI 是漆在地上與牆上的標記。

| 部件 | 在停車場裡是什麼 | 在站上做什麼 |
|---|---|---|
| 3D 場景 | 停車場本體 | 全幕固定 canvas，敘事與操作都發生在裡面 |
| 標題／幕卡 | 牆上的樓層漆字、告示 | 大字 grotesk 全大寫，像漆上去的 |
| 挪車計數 | 收費亭的帳單 | mono 讀數，黃色 |
| 操作台 | 管理員的按鈕盤 | 底部控制列，方角按鈕 |
| 紀錄簿（BST） | 出入登記簿 | 側欄清單，車牌中序排序 |

## 色彩（Restrained：夜色＋單一標線黃）

```
--asphalt #141416   頁底（夜間柏油）
--concrete #26262a  面板、卡片底（混凝土）
--line    #E9E6DF   主文字（標線白）
--muted   #9B978E   次要文字（褪色標線；對比守 4.5:1）
--paint   #F2B824   標線黃。只給：挪車讀數、當前狀態、警示線、關鍵詞
```

**禁止**：第二個強調色、漸層裝飾、發光暈、玻璃模糊、圓角卡片（方角＋1px 線）、
紫色系、密集卡片牆。夜色是「場景的光」，不是「深色模式模板」。

## 字體

- Display／標題：**Archivo** 700/800，全大寫＋letter-spacing .02em（工業告示；站主在盤口筆記選過的路線）。
- 內文：**Noto Sans TC** 400/700。
- 讀數／車牌／圖說：ui-monospace 系統栈，大寫，10–12px。車牌一律 mono。

## 3D 材質語彙

車模＝**自建的 W202（`models/w202.glb` sedan、`w202t.glb` T-Modell 旅行車）**，
全部在 Blender 下料，尺寸照實車：全長 4487 / 全寬 1720 / 全高 1414 / 軸距 2690mm、
輪胎 195/65R15、輪距前 1473 後 1457。車身以 26 道斷面放樣，每條折線放成對支撐斷面；
玻璃與黑柱是材質分區，DLO 邊界隨高度斜走（A 柱下前上後、C 柱下後上前）。
身障車位固定給旅行車；車漆逐台換色（只複製 `W202_Paint` 一顆材質）。
GLB 載入失敗自動退回手拼方盒車（同檔案內 buildBoxCar）。
**不用 Kenney Car Kit**：那組車的問題不是顏色是比例（矮、圓、無底盤無門縫），
上一版整支 `nightPalette()` 在重調它的色票，是在幫玩具化妝。

停車場＝**`models/lot.glb`（同一份 Blender 場景）**：地坪、標線幾何、圓端緣石島、
車擋、圍牆（含壓頂石與壁柱）、眼鏡蛇燈桿、收費亭、進出道閘、車阻柱、排水溝、P 字柱。
標線畫成真幾何（不是貼圖），座標直接抄 `alleySlot/disSlot/flatSlot/queueSlot`。
柏油是 Blender 程序生成的貼圖：輪胎輾痕（沿 `driveTo` 的動線）、油漬、補丁、骨料雜訊——
全新的黑板就是 CG 感的來源。中文漆字仍留在 canvas 疊層（字型進不了 glTF）。
光影：冷月光（castShadow, PCFSoft 2048）＋一排暖黃鈉燈＋ACES 調色＋
RoomEnvironment 低強度環境反射。不做霧、不做粒子、不做後製 bloom。

## 動態

- **唯一 authored moment：挪車之舞**——取深處車時，前車逐台倒出→目標出場→逐台歸位，
  帳單讀數逐次跳。其他動畫都為它服務。
- **挪車順序永遠由引擎即時算，不烘成 Blender 動畫**：挪幾台取決於當下堆疊狀態，
  烘成固定影格＝這個展覽的論點死掉，變成一支影片。Blender 只出幾何與材質。
- 行進時四輪真的在轉（滾動角＝距離÷輪半徑），轉彎時前輪打角（三角形波，兩端回正）。
- 車進出時道閘抬桿（`gate_in_arm`／`gate_out_arm` 是 GLB 裡的獨立節點）。
- 鏡頭走位跟著幕走（高空總覽→巷道側視→操作台 3/4 視角），lerp，不甩鏡。
- **直式有自己一組鏡頭（`POSES_P`），不是把橫式機位往後拉**。拉遠不會改變主體在畫面上
  的落點——舊版 `mob` 只乘 1.45，結果操作台鏡頭下 18 個主體只有 4 個看得到、巷道 5 格
  全部被程式字條蓋住。直式要換**方位角**：從東南／西南看，平面排往深度延伸而不是橫跨
  畫面，才吃得到直式的高度。命中數（375×667 實測投影）：alley 9/9、duel 12/12、
  queue 6/7、console 15/18（平面排最遠三格仍在框外，是 375px 寬的先天限制）。
- 改鏡頭或改手機版面時，**兩件事要一起改**：可用 3D 帶 = mast 底 到 底部堆疊頂。
  帶變了鏡頭就要重解，只改一邊等於沒改。
- `prefers-reduced-motion`：tween 時長歸零，直接呈現結果狀態。

## 尺度（改地坪或車模前先讀這段）

| 量 | 站上單位 | 換算 |
|---|---|---|
| 車長 | 2.60 | 實車 4487mm → 1 公尺 ＝ 0.58 站上單位 |
| 車寬 | 1.00 | 實車 1720mm |
| 車格 | 1.62 × 2.90 | 實車車位 2.5 × 5.0m |
| 巷道車位間距 | 2.90（`alleySlot` 固定） | 車長 2.6 → 車距 0.3 |

**車格一定要照車量**：上一版車格是 2.5 × 3.4（照 Kenney 玩具車的胖比例畫的），
換上真比例的車之後只填得滿四成格寬，看起來像小孩坐大人椅。
`alleySlot／disSlot／flatSlot／queueSlot` 的座標不能動（動畫靠它），
但**地上畫多寬是自由的**——不合就改標線，不要改座標。

## 憲法（不可協商）

- 挪車數字由引擎真算。引擎邏輯忠於 `_src/*.py`。
- **挪車動畫不烘進 Blender**：挪幾台是 runtime 依堆疊狀態算的，烘成固定影格＝論點死掉。
  Blender 出幾何、材質與機構（輪子節點、道閘臂桿）；順序與動線留給引擎。
- 車牌合成資料要標明。375px 無水平捲軸。單檔 HTML，Three.js 是唯一外部 JS。
- 左右對照（巷道堆疊 vs 平面隨機存取）是論點本體，不得為了版面刪掉。
